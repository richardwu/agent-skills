# Agent Instructions

Always keep the skill list in `README.md` up to date with the skill directories in `skills/`.

When adding, removing, or renaming a skill directory, update the README in the same change.
